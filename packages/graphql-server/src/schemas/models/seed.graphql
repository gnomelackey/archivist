type Seed {
  id: String!
  type: String!
  value: String!
  user: String!
  factionAlliances: [FactionAlliance]
  factionConflicts: [FactionConflict]
  createdAt: DateTime!
  updatedAt: DateTime!
}

type SeedType {
  type: String!
  seeds: [Seed!]!
}
