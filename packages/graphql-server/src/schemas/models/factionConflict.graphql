type FactionConflictInput {
  reason: String
  aggressor: String!
  defender: String!
  seed: String
}

type FactionConflict {
  id: String!
  reason: String
  aggressor: String!
  defender: String!
  seed: String
  createdAt: DateTime!
  updatedAt: DateTime!
}
